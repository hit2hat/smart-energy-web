группа какая-то

```
const Example = () => {
    return (
        <Group>
            <div 
                style={{
                    height: 250,
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'center'
                }}
            >
                здесь контент
            </div>
        </Group>
    );
}

Example();
```